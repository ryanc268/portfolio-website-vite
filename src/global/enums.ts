export enum TrackDirection {
  FORWARD,
  BACK,
}

//If more are added their logic will need to be added to the AudioVisualizer switch case (Default BASIC)
export enum Visualizer {
  BASIC,
  WAVEFORM,
  PARTY,
  SHARP,
  BLOCK,
  CHAOS,
  CIRCLE_BAR,
  REFLECTED_BAR,
}
